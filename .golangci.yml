version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - gocritic
    - misspell
  exclusions:
    rules:
      - path: _test\.go
        linters: [govet]

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
